spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        #nacos注册中心地址
        server-addr: http://110.42.202.164:8848
        #默认为Public命名空间,不需要写，如果加上的话会导致加载不到配置文件
        namespace: f6370e63-18e4-4680-b46e-c0378e46308d
        #指定文件后缀
        file-extension: ymal
        #指定分组信息
        group: