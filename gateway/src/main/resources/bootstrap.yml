spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        #nacos注册中心地址
        server-addr:
        #默认为Public命名空间,不需要写，如果加上的话会导致加载不到配置文件
        namespace:
        #文件名 -- 如果没有配置则默认为 ${spring.appliction.name}
        prefix:
        #指定文件后缀
        file-extension:
        #指定分组信息
        group: